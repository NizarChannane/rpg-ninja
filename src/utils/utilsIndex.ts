import * as grid from "./grid";
import * as collisions from "./collisions";
import * as inputs from "./inputs";
import * as gameEvents from "./gameEvents";
import * as characterBehaviors from "./characterBehaviors";

const utils = {
    grid,
    collisions,
    inputs,
    gameEvents,
    characterBehaviors
};

export default utils;